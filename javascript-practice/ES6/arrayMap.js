const colors = ["red", "green", "blue"];
const items = colors.map((color) => `<li>${color}</li>`); //esto es llamado template literal
// el metodo transforma cada elemento del array, toma un item y devuelve ese item